#include <bits/stdc++.h>
#define N 105
using namespace std;
bool lo[N];
typedef pair<int,int> pii;
typedef struct bt{
    int data;
    bt *lc,*rc;
}
vector<pii> vpi(N);
int main()  {
    int n;
    scanf("%d",&n);
    for (int i=0;i<n;i++)   {
        pii npi;
        scanf("%d%d",&npi.first,&npi.second);

    }
    return 0;
}
