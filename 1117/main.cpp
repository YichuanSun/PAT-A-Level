#include <bits/stdc++.h>
#define N 100005
using namespace std;
int a[N];
bool usd[N]
int main(){
    int n,fl=N,ce=-1,frq=-1,ans=0;
    scanf("%d",&n);
    for (int i=0;i<n;i++)   {
        scanf("%d",a+i);
        fl=min(fl,a[i]);
        ce=max(ce,a[i]);
    }
    for (int i=0;i<n;i++)   {

    }
    return 0;
}
